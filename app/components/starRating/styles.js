import {color, fontSize, size} from 'theme';

export const container = () => ({
  flexDirection: 'row',
  paddingRight: size.moderateScale(5),
});
export const singleStar = () => ({
  paddingRight: size.moderateScale(2),
});
